---
const { tag, url, path } = Astro.props;
---

<a
  class={`block rounded-full px-3 hover:opacity-70 ${url.includes(tag) ? "bg-tag-selected" : "bg-tag-default"}`}
  href={`/${path}/${tag}/`}
>
  {tag}
</a>
